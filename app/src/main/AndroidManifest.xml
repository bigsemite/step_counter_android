<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bigsemite.stepper">
    <uses-feature android:name="androir.hardware.Sensor.stepdetector"></uses-feature>
    <uses-feature android:name="android.hardware.Sensor.stepcounter"></uses-feature>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/runner"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/runner"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>